import { useParams } from 'react-router-dom';

const HorseRecord = () => {
  const { horseId } = useParams();

  return (
    <div>
      <h1>Horse Record</h1>
    
  
    </div>
  );
};

export default HorseRecord;
